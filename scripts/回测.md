现在我要动态的对BUY_CONDITIONS_CONFIG进行赋值，以便于获取到盈利率最高的买入点策略。具体的方案如下：

step1、先全部初始化
	1、TIME_FROM_CREATION_MINUTES 有以下几个值 2,3,4,5
	2、NOWSOL_RANGE 有以下几个分组为 (5, 15) (5, 30) (15,15)  
	3、TRADE_AMOUNT_RANGE 默认为 (0.3, 2.0)
	4、TIME_DIFF_CHECK_MODE，默认为 debug
	5、FILTERED_TRADES_MIN_AMOUNT 默认有0.05 ,0.1
	6、FILTERED_TRADES_COUNT 默认有 7和10
	7、FILTERED_TRADES_SUM_RANGE 默认分组有 (-6.0, -1.0),(-6.0, -3.0),(-3.0, -1.0)
	8、默认TRADE_TYPE 为buy
	9、MAX_AMOUNT_CHECK_MODE 默认为debug
	10、PRICE_VOLATILITY_CHECK_MODE 默认为debug
	11、TIME_VOLATILITY_CHECK_MODE 默认为debug
	12、AMOUNT_VOLATILITY_CHECK_MODE 默认为debug
	13、PRICE_RATIO_CHECK_MODE 默认为debug
	14、SELL_COUNT_CHECK_MODE 默认为debug
	15、BUY_COUNT_CHECK_MODE 默认为debug
	16、SELL_COUNT_CHECK_MODE 默认为debug

step2、对TIME_FROM_CREATION_MINUTES，NOWSOL_RANGE，FILTERED_TRADES_MIN_AMOUNT、FILTERED_TRADES_COUNT、FILTERED_TRADES_SUM_RANGE的不同值进行分组回测。

先取每个条件的第一个值,进行回测，看是否满足 总交易数>1000,总盈利大于10个sol,胜率大于35%，平均盈利率大于1%。如果不满足，则依次更换条件的值进行回测并保存回测结果的快照信息(debug模式下的分桶的交易数据)。最后将回测完的结果，筛选出来盈利数大于600的组合和回测的快照进入到step3.


step3、将step2返回的满足条件的组合信息，从debug数据里，对TIME_DIFF_CHECK_MODE 、把大于0%的



[44/144] TFC_MINUTES=3, NOWSOL_RANGE=(5, 15), FT_MIN_AMOUNT=0.1, FT_COUNT=7, FT_SUM_RANGE=(-6.0, -3.0)

